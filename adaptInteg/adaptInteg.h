

double adaptInteg(
	double f(double),
	double a,
	double b,
	double abs,
	double eps,
	double *err
);

double adaptIntegWithInf(
	double f(double),
	double a,
	double b,
	double abs,
	double eps,
	double *err
);
